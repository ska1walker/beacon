apiVersion: v2
name: beacon
description: KI-gestütztes CRM für den Vertrieb — läuft vollständig auf der eigenen Box
type: application
version: 0.5.1          # Chart-Version (Semver) — bei jeder Chart-Änderung erhöhen
appVersion: "0.5.1"     # MUSS gleich version sein: der Linter prüft es, und der
                        # wirksame Image-Tag hängt daran (siehe values.yaml)
